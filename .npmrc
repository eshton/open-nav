; Provenance is deliberately *not* set here. It requires a supported CI runner
; with an OIDC token, and a local `npm publish` fails outright when it is on.
; The release workflow sets NPM_CONFIG_PROVENANCE=true instead, so a release
; from CI is attested and a release from a laptop still works.
